#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

override_dh_auto_build:
	# Nothing to build - this is a pure JavaScript/HTML/CSS package

override_dh_auto_test:
	# No tests to run

override_dh_auto_install:
	# Install files using the debian/install file
	dh_install
